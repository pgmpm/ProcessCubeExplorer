namespace pgmpm.ConformanceChecking
{
    /// <summary>
    /// Shows the relationship between two cells in the matrix
    /// </summary>
    /// <autor>Andrej Albrecht</autor>
    public enum CellType
    {
        NotExist,
        Nothing,
        Right,
        Left,
        Parallel,
        Loop
    };
}
